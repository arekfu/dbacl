#!/bin/sh
# header parsing test
# many of the header examples in sample.headers* are copied from
#Bruce Lilly's mparse testing suite http://users.erols.com/blilly/mparse
PATH=/bin:/usr/bin
DBACL=$TESTBIN/dbacl
PCHECK=$TESTBIN/pcheck

cat ${sourcedir}/sample.headers-2821b | $PCHECK 2821 bad
